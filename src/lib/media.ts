/** Must match the `phone` custom variant in globals.css: phones, and tablets held upright. */
export const PHONE_QUERY = "(max-width: 1024px) and ((max-width: 760px) or (orientation: portrait))";
export const REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)";
